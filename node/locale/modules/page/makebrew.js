import { transferLangConfig } from '../../config.js'

const config = {
  // Tab Names
  "title": ["自制内容编辑器", "自制内容编辑器" ,"Homebrew Builder"],
  "subtitle": ["在左侧编辑，在右侧查看结果", "" ,"Input values on left, view the results on the right."],

}

export default transferLangConfig(config)