import { transferLangConfig } from "../../config.js";

const config = {
	// feats
	"repeatable": ["复选", "可重複", "Repeatable"],
	"not_repeatable": ["不可复选", "不可重複", "Not Repeatable"],

};

export default transferLangConfig(config);