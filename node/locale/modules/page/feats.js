import { transferLangConfig } from "../../config.js";

const config = {
	// feats
	"repeatable": ["可重复", "可重複", "Repeatable"],
	"not_repeatable": ["不可重复", "不可重複", "Not Repeatable"],

};

export default transferLangConfig(config);