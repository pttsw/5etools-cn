import { transferLangConfig } from "../../config.js";

const config = {
	// js/enconterbuilder.js
	"rules_note": ["基于遭遇构筑规则构建，源于", "基于遭遇构筑规则构建，源于", "Based on the encounter building rules in the"],
	// button
	"back_to_stat_blocks": ["返回数据卡", "返回統計卡", "Back to Stat Blocks"],
	"shift_for_multi_line_format": ["按住SHIFT多行格式", "按住SHIFT多行格式", "Shift for Multi-Line Format"],
	"shift_to_reset_players": ["按住SHIFT重置玩家", "按住SHIFT重置玩家", "Shift to Reset Players"],
};

export default transferLangConfig(config);