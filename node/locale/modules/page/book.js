import { transferLangConfig } from '../../config.js'

const config = {
    "page_subtitle": ["浏览内容。按F搜索，按G跳转页面。", "浏览内容。按F搜索，按G跳转页面。", "Browse content. Press F to find, and G to go to page."],
}

export default transferLangConfig(config)